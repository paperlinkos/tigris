import 'package:flutter/material.dart';

class AppColors {
  // Warm off-white background system (Light Mode)
  static const Color background = Color(0xFFFAF8F5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceSubtle = Color(0xFFF3EFEA);

  // Black primary text & neutral greys (Light Mode)
  static const Color textPrimary = Color(0xFF141311);
  static const Color textSecondary = Color(0xFF75726D);
  static const Color textTertiary = Color(0xFFA3A09A);

  // Minimal borders & dividers (Light Mode)
  static const Color border = Color(0xFFE9E5DE);
  static const Color borderSubtle = Color(0xFFF0ECE5);

  // Restrained accent (Light Mode)
  static const Color accent = Color(0xFF2C2A26);
  static const Color accentSubtle = Color(0xFFE5DFD5);

  // Deep Obsidian & Warm Charcoal system (Dark Mode)
  static const Color darkBackground = Color(0xFF141312);
  static const Color darkSurface = Color(0xFF1E1C1A);
  static const Color darkSurfaceSubtle = Color(0xFF2A2724);

  // Soft off-white text & muted greys (Dark Mode)
  static const Color darkTextPrimary = Color(0xFFEFECE6);
  static const Color darkTextSecondary = Color(0xFFA8A49E);
  static const Color darkTextTertiary = Color(0xFF6E6A63);

  // Minimal dark borders & dividers (Dark Mode)
  static const Color darkBorder = Color(0xFF2C2925);
  static const Color darkBorderSubtle = Color(0xFF23201D);

  // Restrained dark accent (Dark Mode)
  static const Color darkAccent = Color(0xFFFAF8F5);
  static const Color darkAccentSubtle = Color(0xFF383430);
}
